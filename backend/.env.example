# 백엔드 서버 포트
PORT=3001

# SQLite 파일 저장 디렉터리 (Railway에서는 마운트된 볼륨 경로로 설정)
# Railway: /app/data
# 로컬: 기본값(./data) 사용 시 생략 가능
DB_DIR=./data

# 허용할 프론트엔드 URL (Vercel 배포 후 입력)
FRONTEND_URL=https://your-project.vercel.app
